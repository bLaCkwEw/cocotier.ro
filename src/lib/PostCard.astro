---
import Post from "@lib/Post.astro";
import Card from "@lib/design-system/components/Card.astro";

const { post } = Astro.props;
---

<Card href={`/blog/${post.id}/`}>
	<Post title={post.data.title} date_pub={post.data.date_pub} />
</Card>
